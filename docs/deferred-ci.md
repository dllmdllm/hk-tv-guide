# Deferred CI note

Token lacks workflow scope; apply fetch then test:data then data commit then test:build manually to update-and-deploy.yml after merge.
scripts/ci-warn-partial.mjs is included in this PR.
